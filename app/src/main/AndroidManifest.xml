<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.itborneo.ugithub">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="id.itborneo.ugithub.READ_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ugithub">
        <receiver android:name=".widget.UgithubWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ugithub_widget_info" />
        </receiver>

        <activity android:name=".detail.DetailActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver
            android:name=".core.utils.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <provider
            android:name=".core.local.FavoriteContentProvider"
            android:authorities="id.itborneo.ugithub"
            android:enabled="true"
            android:exported="true"
            android:readPermission="id.itborneo.ugithub.READ_DATABASE" />
    </application>

</manifest>